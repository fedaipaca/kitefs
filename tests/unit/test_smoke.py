"""Smoke tests to verify the test runner works and the package is importable.

These placeholder tests exist solely to ensure `just test` (pytest) exits
successfully while the real test suite is being developed. Remove this file when actual tests are placed.
"""


def test_placeholder_passes() -> None:
    """Trivial assertion so pytest always collects at least one passing test."""
    assert True
